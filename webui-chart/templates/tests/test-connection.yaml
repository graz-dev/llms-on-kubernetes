apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webui-chart.fullname" . }}-test-connection"
  labels:
    {{- include "webui-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webui-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
