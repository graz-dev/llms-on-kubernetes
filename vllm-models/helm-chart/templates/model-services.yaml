{{- range .Values.models }}
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-{{ .modelName }}
  labels:
    app: vllm-{{ .modelName }}
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: vllm-{{ .modelName }}
{{- end }}