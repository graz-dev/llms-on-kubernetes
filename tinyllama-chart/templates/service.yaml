apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinyllama-chart.fullname" . }}
  labels:
    {{- include "tinyllama-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "tinyllama-chart.selectorLabels" . | nindent 4 }}
